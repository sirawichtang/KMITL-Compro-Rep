#include <stdio.h>



/*
#include <stdio.h>
#include <string.h>
int main()
{
    int i;
    struct profile
    {
        char name[10];
        int age;
    } ID[10];

    for (i = 1; i <= 10; i++)
    {
        printf("Student[%d]\n", i);
        printf("\tname = ");
        scanf("%s", ID[i].name);
        printf("\tage = ");
        scanf("%d", &ID[i].age);
    }
    for (i = 1; i <= 10; i++)
    {
        if (ID[i].age >= 20)
        {
            printf("\n%s, %d\n", ID[i].name, ID[i].age);
        }
    }
    return 0;
}

*/